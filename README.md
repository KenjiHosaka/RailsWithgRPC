# RailsWithgRPC
gRPC (go) + Rails

# Environment
- rails 5.2.0
- ruby 2.5.0

# Useage

```bash
$ docker-compose build
$ docker-compose run --rm app rake db:create
$ docker-compose up
```
access to localhost:3000

